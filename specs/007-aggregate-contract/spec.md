# Feature 007: Aggregate Analysis Contract, Ontology Vendoring, Boundary Enforcement

**Status**: implemented 2026-09-02; the `machine/rdam` layout described below was superseded the same day by the owner's single-package ruling — `rdam/` now sits at the repository root ([010 §Single package](../010-repository-migration/spec.md)) | **Authority**: [006 spec](../006-rhetorical-discourse-machine/spec.md), [006 data model](../006-rhetorical-discourse-machine/data-model.md), [006 contracts](../006-rhetorical-discourse-machine/contracts/) | **Owner ruling**: "build it all" (2026-09-02)

This feature builds what 006 specified and deliberately did not build: the machine layer.
The requirements are 006's; this record maps each to the artifact that satisfies it and
the gate that proves it. Nothing here re-decides a 006 ruling.

## Deliverables

| Requirement | Artifact | Proof |
|---|---|---|
| Aggregate contract in `machine/` (006 research D1; FR-012, FR-013, FR-014) | `machine/rdam/` — import name `rdam`; `contracts.py` types every entity of the 006 data model: `ProviderDeclaration`, `FormalismDeclaration`, `CapabilityState`, `NativeTechniqueResult`, `Outcome`, `AggregateAnalysis`, `ProviderDependencyReference`, `MachineCapabilities`, `AggregateRequest` | `tests/machine/test_contracts.py` |
| Machine: N explicit outcomes, no suppression, no stubs, no retries (FR-014, FR-020, SC-005, SC-007, SC-010) | `machine/rdam/machine.py` — `Machine.capabilities()` reads declarations only; `Machine.analyse()` yields one outcome per requested technique | `tests/machine/test_machine.py` — including "withholding one provider leaves every other declaration byte-identical" (SC-010) and "a typed failure never suppresses another success" (SC-005) |
| Formalism ruling (006 U1): one provider, several formalisms, each with its own `coe:` identity | `FormalismDeclaration`; the RST fixture declares `rst_tree → …/rst`, `erst_graph → …/erst` | `test_contracts.py::test_rst_declares_erst_as_a_second_formalism_with_its_own_identity` |
| Canonical identity binding, referenced never redefined (FR-002; 006 research D6) | `ontology/vendor/central-configs/` (Central staged distribution, 53 files, `ontology-check` green at Central before staging); `ontology/schema/rdam.linkml.yaml` importing `coe.linkml`; `ontology/data/rdam-providers.yaml` binding all seven boundaries and the two RST formalisms; packaged projection `machine/rdam/resources/framework-identities.json` generated by `tools/ontology/project_framework_identities.py` | `pixi run ontology-validate` (lint, schema, instances, projection currency); `tests/machine/test_frameworks.py` asserts the projection equals a fresh projection of the vendored taxonomy and every curie is a vendored concept id |
| D5 check (a): production never imports `workbench.*`, transitively (FR-006, SC-003) | `tools/production_boundary/imports.py` — boundary roots `machine`, `rst`, `dung`, `ibis` are production source roots; boundary directories are not packages (`machine/rdam/x.py` is module `rdam.x`) | `tests/integration/test_production_boundary.py::test_machine_boundary_package_imports_by_its_own_name_and_may_not_reach_workbench` |
| D5 check (b): no distributable carries a `workbench/` member (FR-006, SC-003) | `tools/production_boundary/artifacts.py` — production wheels may carry only `isanlp_rst/`, `rdam/`, `rdam_dung/`, `rdam_ibis/` roots | `…::test_machine_wheel_members_are_inside_the_boundary_and_workbench_still_is_not` |
| Every boundary has exactly one owner (SC-001) | `tools/production_boundary/authority.py` — rules for `machine`, `rst`, `dung`, `ibis` (production, publishable) and `ontology` (repository) | `…::test_machine_boundary_and_ontology_have_exactly_one_owner`; `pixi run -e default production-boundary` valid |
| Retryability standard (006 capability contract) | `Retryability` mandatory on `ProviderFailure`; `UnavailableOutcome` has no retryability by construction; the machine never retries | `test_machine.py::test_a_typed_failure_never_suppresses_another_success` asserts a single call |
| Internal bugs propagate, never relabelled (pattern P9) | `Machine` catches only `ProviderError` | `test_machine.py::test_an_unexpected_exception_is_a_bug_and_propagates` |

## Package facts

`rdam` 1.0.0 (`machine/pyproject.toml`), depends on `pydantic` and `rfc8785` only; it does
not import `isanlp_rst`. Installed editable in the `default` and `production` pixi
environments. Contract identifiers: `rdam.aggregate`, `rdam.capabilities`,
`rdam.native_result`, all at contract version `1.0.0`; records serialize as RFC 8785
canonical JSON with a self-checking `semantic_digest` (`rdam.serialization`).

## Not in this feature

Cross-provider orchestration (lineage is a validated field the machine does not yet
populate; 006 orders it last) and the shared analysis store ruling named in the 006
standardised-patterns register — both remain future features.

## Gates

See [evidence/gates.md](evidence/gates.md).
